Opções de sdk-options
-d ou --diagnostics: Habilitar saída de diagnóstico.
-h ou --help: Mostrar a ajuda da linha de comando.
--info: Exiba informações do .NET.
--list-runtimes: Exiba os runtimes instalados.
--list-sdks: Exiba os SDKs instalados.
--version: Exiba a versão do SDK do .NET em uso.
Comandos do SDK
Aqui estão alguns comandos do SDK do .NET que podem facilitar o seu desenvolvimento:

add: Adicione um pacote ou referência a um projeto do .NET.
build: Crie um projeto do .NET.
clean: Limpe as saídas do build de um projeto do .NET.
format: Aplique preferências de estilo a um projeto ou solução.
list: Liste as referências de um projeto do .NET.
msbuild: Execute comandos do MSBuild (Microsoft Build Engine).
new: Crie um novo projeto ou arquivo do .NET.
nuget: Forneça comandos adicionais do NuGet.
pack: Crie um pacote do NuGet.
publish: Publique um projeto do .NET para implantação.
restore: Restaure as dependências especificadas em um projeto do .NET.
run: Crie e execute uma saída de projeto do .NET.
sdk: Gerencie a instalação do SDK do .NET.
sln: Modifique os arquivos da solução do Visual Studio.
store: Armazene os assemblies especificados no repositório de pacotes do runtime.
test: Execute testes de unidade usando o executor de testes especificado em um projeto do .NET.
tool: Instale ou gerencie ferramentas que aprimoram a experiência do .NET.
workload: Gerencie cargas de trabalho opcionais.
Comandos Adicionais de Ferramentas em Pacote
Aqui estão alguns comandos adicionais de ferramentas em pacote que podem ser úteis:

dev-certs: Crie e gerencie certificados de desenvolvimento.
fsi: Inicie o F# Interativo ou execute scripts do F#.
sql-cache: Ferramentas de cache do SQL Server em linha de comando.
user-secrets: Gerencie segredos do usuário para desenvolvimento.
watch: Inicie um observador de arquivo que executa um comando quando os arquivos são alterados.
Exemplo de Uso de dotnet new
O comando dotnet new é usado para criar um novo projeto .NET com base em modelos predefinidos. Aqui estão alguns exemplos de modelos comuns que você pode usar:

Aplicativo do Console: Crie um aplicativo de console simples.

Exemplo: dotnet new console
Aplicativo do Windows Forms: Crie um aplicativo Windows Forms (para interfaces gráficas do Windows).

Exemplo: dotnet new winforms
Aplicativo WPF (Windows Presentation Foundation): Crie um aplicativo WPF para criar interfaces gráficas ricas para desktop.

Exemplo: dotnet new wpf
ASP.NET Core Web App: Crie um aplicativo web ASP.NET Core.

Exemplo: dotnet new webapp
Biblioteca de Classes: Crie uma biblioteca de classes reutilizável.

Exemplo: dotnet new classlib
Blazor Server App: Crie um aplicativo Blazor Server para desenvolvimento web interativo.

Exemplo: dotnet new blazorserver